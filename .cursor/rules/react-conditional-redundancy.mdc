---
description: Evitar condições redundantes em TSX (reduz findings e ruído)
globs: resources/js/**/*.tsx
alwaysApply: false
---

# Condições redundantes (TSX)

- **Evite repetir guardas dentro do mesmo bloco**:

```tsx
{canManage && (
  <>
    {/* ❌ redundante (sempre true aqui) */}
    {canManage && <Item />}
  </>
)}
```

